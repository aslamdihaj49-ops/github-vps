name: VPS
on: [workflow_dispatch]
jobs:
  vps:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Hermes
        run: |
          sudo apt update && sudo apt upgrade -y
          sudo apt install -y nodejs npm curl wget git
          curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash
          source ~/.bashrc
          hermes config set provider openrouter
          hermes config set model nousresearch/hermes-3-llama-3.1-405b:free
          export TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_TOKEN }}
          echo -e "Y\nY" | hermes gateway install
          hermes gateway start
          echo "Hermes Agent يعمل الآن!"
          sleep 6h
